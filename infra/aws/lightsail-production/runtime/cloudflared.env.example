# Legacy placeholder retained to make old copies fail safely. The production
# unit does not read an environment file. Store the real tunnel token only at
# /etc/saveswitch/runtime/cloudflared-token as root:root mode 0600; systemd
# passes it through LoadCredential and cloudflared reads it with --token-file.
