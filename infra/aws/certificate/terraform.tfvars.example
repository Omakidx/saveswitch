aws_account_id = "123456789012"
api_domain     = "api.saveswitch.xyz"

# Populate only after the Cloudflare owner creates the records returned by
# dns_validation_records. A second, separately approved apply completes ACM.
validation_record_fqdns = []
