interface:
  display_name: "AWS Migration"
  short_description: "Build and validate safe AWS migration IaC"
  default_prompt: "Use $aws-migration to refine the AWS target and produce a safe, repository-scoped IaC readiness assessment."
policy:
  allow_implicit_invocation: false
