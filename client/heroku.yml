build:
  docker:
    web: Dockerfile
  config:
    NEXT_PUBLIC_API_BASE: https://api.saveswitch.xyz
    NEXT_PUBLIC_API_URL: https://api.saveswitch.xyz

run:
  web: node node_modules/next/dist/bin/next start -p ${PORT:-5173}
